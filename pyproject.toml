[build-system]
requires = []
build-backend = "madrona-py-build"
backend-path = ["external/madrona/py"]

[project]
name = "madrona_renderer"
version = "0.0.1"

[tool.madrona.packages.madrona_renderer]
ext-only = true
ext-out-dir = "build"
